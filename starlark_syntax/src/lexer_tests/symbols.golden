# @generated
# To regenerate, run:
# ```
# STARLARK_RUST_REGENERATE_GOLDEN_TESTS=1 cargo test -p starlark_syntax --lib
# ```

Program:
, ; : += -= *= /= //= %= == != <= >= ** = < > - + * % / // . { } [ ] ( ) |
,;:{}[]()|...

Tokens:
symbol ','    # ,
symbol ';'    # ;
symbol ':'    # :
symbol '+='   # +=
symbol '-='   # -=
symbol '*='   # *=
symbol '/='   # /=
symbol '//='  # //=
symbol '%='   # %=
symbol '=='   # ==
symbol '!='   # !=
symbol '<='   # <=
symbol '>='   # >=
symbol '**'   # **
symbol '='    # =
symbol '<'    # <
symbol '>'    # >
symbol '-'    # -
symbol '+'    # +
symbol '*'    # *
symbol '%'    # %
symbol '/'    # /
symbol '//'   # //
symbol '.'    # .
symbol '{'    # {
symbol '}'    # }
symbol '['    # [
symbol ']'    # ]
symbol '('    # (
symbol ')'    # )
symbol '|'    # |
new line      # \n
symbol ','    # ,
symbol ';'    # ;
symbol ':'    # :
symbol '{'    # {
symbol '}'    # }
symbol '['    # [
symbol ']'    # ]
symbol '('    # (
symbol ')'    # )
symbol '|'    # |
symbol '...'  # ...
new line      #
